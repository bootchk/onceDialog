#!/usr/bin/env python

from distutils.core import setup

setup(name='onceDialog',
      version='0.1',
      description='once per installation (license) dialog for Qt, Python',
      author='Lloyd K. Konneker',
      author_email='bootch@nc.rr.com',
      url='https://github.com/bootchk/onceDialog',
      license='LGPLv3+',
      packages=['onceDialog', 'doc'],
      )
